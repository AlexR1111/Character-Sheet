export function D20(min = 1, max = 20) {
    return Math.floor(Math.random() * (max - min + 1)) + min;
}

export function D12(min = 1, max = 12) {
    return Math.floor(Math.random() * (max - min + 1)) + min;
}

export function D10(min = 1, max = 10) {
    return Math.floor(Math.random() * (max - min + 1)) + min;
}

export function D8(min = 1, max = 8) {
    return Math.floor(Math.random() * (max - min + 1)) + min;
}

export function D6(min = 1, max = 6) {
    return Math.floor(Math.random() * (max - min + 1)) + min;
}

export function D4(min = 1, max = 4) {
    return Math.floor(Math.random() * (max - min + 1)) + min;
}
